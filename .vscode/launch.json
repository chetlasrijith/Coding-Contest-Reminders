{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Scheduler",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/src/scheduler_main.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
